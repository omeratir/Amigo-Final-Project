import { Place } from '../places/place.model';

export interface Route {
  id: string;
  name: string;
  places: string;
  content: string;
  time_of_route: string;
  rating: string;
  creator: string;
}
